@{
    Layout = null;
    var webpageContent = File.ReadAllText(Server.MapPath("~/portal/index.html"));
}

@Html.Raw(webpageContent.Replace("<!-- BOOTSTRAP SETTINGS -->", @"<script>window.bootstrapSettings = " + ViewBag.Settings + ";</script>"))

