/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AdatePipe } from './adate.pipe';

describe('AdatePipe', () => {
  it('create an instance', () => {
    const pipe = new AdatePipe();
    expect(pipe).toBeTruthy();
  });
});
